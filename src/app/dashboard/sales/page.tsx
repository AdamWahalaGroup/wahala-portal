/**
 * Sales index — the board itself lives in layout.tsx (persistent). This page is the
 * "no drawer" state, so it renders nothing; child segments (deals/leads/proposals)
 * render the drawer overlay over the board.
 */
export const dynamic = "force-dynamic";

export default function SalesIndexPage() {
  return null;
}
