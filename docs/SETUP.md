# Setup

Phase 0 setup now lives in **[`PHASE-0.md`](PHASE-0.md)** — the authoritative, current Cloudflare skeleton guide:
Next.js (App Router) on **Cloudflare Workers via OpenNext**, with **D1 (Drizzle) + KV (sessions + magic-link tokens) + R2 (files) + Cloudflare Email (magic-link login) + Stripe test mode**. Everything on Cloudflare — no AWS, no external identity provider.

Follow `PHASE-0.md`.

> This file previously described an AWS Cognito setup; that was superseded by the fully-Cloudflare magic-link approach in `PHASE-0.md`.
